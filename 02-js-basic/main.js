// Функции vs процедуры

function sum(a, b) {
    return a + b;
}

const result = sum(4, 6)
console.log(result)

// процедура
function sum2(a, b) {  // процедуры просто выводят какой-то набор операций , но ничего не возвращает, так как нет return
    console.log(result)
}